---

dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: restic_server.debian11-specific_version
    image: geerlingguy/docker-debian11-ansible:latest  # debian 11 - bullseye
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    command: /sbin/init
    pre_build_image: true
    privileged: true

  - name: restic_server.debian12-specific_version
    image: geerlingguy/docker-debian12-ansible:latest  # debian 12 - bookworm
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    command: /sbin/init
    pre_build_image: true
    privileged: true

  - name: restic_server.ubuntu2204-specific_version
    image: geerlingguy/docker-ubuntu2204-ansible:latest  # ubuntu 22.04 - jammy
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    command: /sbin/init
    pre_build_image: true
    privileged: true

  - name: restic_server.ubuntu2404-specific_version
    image: geerlingguy/docker-ubuntu2404-ansible:latest  # ubuntu 24.04 - noble
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    command: /sbin/init
    pre_build_image: true
    privileged: true

  - name: restic_server.rockylinux9-specific_version
    image: geerlingguy/docker-rockylinux9-ansible:latest  # Rocky Linux 9
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    command: /sbin/init
    pre_build_image: true
    privileged: true

  - name: restic_server.rockylinux10-specific_version
    image: geerlingguy/docker-rockylinux10-ansible:latest  # Rocky Linux 10
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    command: /sbin/init
    pre_build_image: true
    privileged: true
provisioner:
  name: ansible
verifier:
  name: ansible
